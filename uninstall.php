<?php

/**
 * Fired when the plugin is uninstalled.
 *
 * @link       https://webkust.be/
 * @since      0.1
 *
 * @package    Custom Google Maps
 */

// If uninstall not called from WordPress, then exit.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
